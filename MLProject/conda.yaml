name: workflow_env
channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.10
  - pip
  - pip:
      - mlflow
      - pandas
      - scikit-learn
      - numpy
      - matplotlib
      - seaborn
